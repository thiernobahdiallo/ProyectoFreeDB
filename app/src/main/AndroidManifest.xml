<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vertice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vertice">
        <activity android:name=".BBDD.SeleccionarAlumno"></activity>
        <activity android:name=".BBDD.DeleteAlumno" />
        <activity android:name=".BBDD.ModificarAlumno" />
        <activity android:name=".BBDD.ListarAlumnos" />
        <activity android:name=".BBDD.AnadirAlumnos" />
        <activity android:name=".Asignaturas" />
        <activity android:name=".Alumnos" />
        <activity android:name=".Menu" />
        <activity android:name=".mosaico.Mosaico" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>